import ScreenPlaceholder from '../components/ScreenPlaceholder';

export default function CalendarScreen() {
  return (
    <ScreenPlaceholder
      title="Calendar"
      subtitle="Training heatmap — workout frequency over time."
    />
  );
}
